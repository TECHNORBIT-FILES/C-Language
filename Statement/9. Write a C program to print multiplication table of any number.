#include<stdio.h>
void main()
{
    int i, n, ans;

    printf("Enter the value of n=");
    scanf("%d", &n);

    for (i = 1; i <= 10; i++)
    {
        ans = n * i;
        printf("%d * %d = %d\n", n, i, ans);
    }
}
