22. Write a C program to create an array of n elements using static  memory allocation to Find Union & Intersection of 2 arrays.
#include <stdio.h>
void main() 
{
    int a[10], n, b[10], m, c[20], d[20];
    
    printf("Enter no of elements in a:\n");
    scanf("%d", &n);
    
    printf("Enter value of elements in a:\n");
    for (i = 0; i < n; i++) 
    {
        scanf("%d", &a[i]);
    }
    printf("Enter no of elements in b:\n");
    scanf("%d", &m);
    
    printf("Enter value of elements in b :\n");
    for (i = 0; i < n; i++) 
    {
        scanf("%d", &b[i]);
    }
    for (i = 0; i < n; i++) 
    {
        flag = 0;
        j = k;
        while (j >= 0) 
        {
            if (arr[j] == arr[i]) 
            {
                flag = 1;
                break;
            }
            j--;
            if (flag == 0) 
            {
                c[k] = a[i];
                k++
            }
        }
    }
}

